{
  "name": "antigravity-arn-lifebar-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Test harness for ARN-Lifebar — devDeps live here and NEVER ship in the .vsix.",
  "scripts": {
    "test": "vscode-test",
    "inspect": "node inspect.mjs"
  },
  "devDependencies": {
    "@vscode/test-cli": "^0.0.10",
    "@vscode/test-electron": "^2.4.1",
    "mocha": "^11.7.5"
  },
  "overrides": {
    "serialize-javascript": "^7.0.5"
  }
}
